package com.example.noteappfirebasecrud.navigation

enum class Screens(val route: String) {
    NoteLandingScreen(route = "landing_screen"),
    NoteScreen(route = "note_screen"),
    NoteEditScreen(route = "note_edit_screen");
}